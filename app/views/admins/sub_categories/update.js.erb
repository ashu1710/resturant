<% if @find_category.present? %>
	toastr.error('Record already present!!!');
<% else %>
	$('.new_category_btn').removeClass("hide");
	$('.panel-body').remove();
	<% if @is_updated %>
		$(".tr_<%= @sub_category.id %>").replaceWith("<%= j render(partial: 'sub_category_data', locals: {sub_category: @sub_category}) %>");
		toastr.success('Record Updated Successfully');
	<% else %>
		toastr.error('Record not updated...');
	<% end %>
<% end %>
onAjaxScroll();
