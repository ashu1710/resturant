<%= render 'layouts/admin_header' %>
<%= render 'layouts/admin_side_bar' %>
<section id="main-content">
     <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Contact Us Table
                    </header>
                    <table class="table table-striped table-advance table-order table-hover" id="dataTables-example">
                        <thead>
                          <tr>
                            <th><i class="icon_profile"></i>First Name</th>
                            <th><i class="icon_profile"></i>Last Name</th>
                            <th><i class="icon_profile"></i>Mobile No.</th>
                            <th><i class="icon_profile"></i>Email Id</th>
                            <th><i class="icon_profile"></i>Message</th>
                          </tr>
                        </thead>
                        <tbody>
                            <%= render partial: "feedback_data", collection: @feedbacks, as: :feedback %> 
                        </tbody>
                    </table>
                </section>
            </div>
        </div>           
    </section>
</section>
