<tr class="tr_<%= size.id %>">
    <td>
        <%= size.size_no %>
    </td>
    <td>
        <div class="btn-group">
            <%= link_to size_path(size), method: :delete, remote: true, class: 'btn btn-danger' do %>
                <i class="icon_close_alt2"></i>
            <% end %>
        </div>
    </td>
</tr>