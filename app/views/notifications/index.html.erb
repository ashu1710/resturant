<%= render 'layouts/admin_header' %>
<%= render 'layouts/admin_side_bar' %>
<section id="main-content">
     <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Notification Table
                    </header>
                    <%= link_to 'New', new_notification_path, remote: true,class: 'btn btn-success new_notification_btn' %>
                    <table class="table table-striped table-advance table-notification table-hover" id="dataTables-example">
                        <thead>
                          <tr>
                            <th><i class="icon_profile"></i>Notification Name</th>
                            <th><i class="icon_cogs"></i> Action</th>
                          </tr>
                        </thead>
                        <tbody>
                            <%= render partial: "notification_data", collection: @notifications, as: :notification %> 
                        </tbody>
                    </table>
                </section>
            </div>
        </div>           
    </section>
</section>
