<%= render 'layouts/admin_header' %>
<%= render 'layouts/admin_side_bar' %>
<section id="main-content">
     <section class="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <section class="panel">
                    <header class="panel-heading">
                        Booking Table
                    </header>
                    <table class="table table-striped table-advance table-booking table-hover" id="dataTables-example">
                        <thead>
                          <tr>
                            <th><i class="icon_profile"></i>Booking Date</th>
                            <th><i class="icon_profile"></i>User mobile No.</th>
                            <th><i class="icon_profile"></i>User email</th>
                            <th><i class="icon_profile"></i>Status</th>
                            <th><i class="icon_cogs"></i> Action</th>
                          </tr>
                        </thead>
                        <tbody>
                            <%= render partial: "booking_data", collection: @list_bookings, as: :booking %> 
                        </tbody>
                    </table>
                </section>
            </div>
        </div>           
    </section>
</section>
